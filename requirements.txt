# Audiblez + Kokoro-82M EPUB-to-Audiobook Pipeline
# Install: pip install -r requirements.txt

# Core TTS
# Note: audiblez 0.4.9 pins kokoro<0.8.0 but kokoro 0.9.4 has better voices.
# We install kokoro first at latest, then audiblez with --no-deps in setup.sh.
kokoro>=0.9.4
audiblez>=0.4.9

# PyTorch with CUDA support
torch>=2.4.0
torchaudio>=2.4.0

# EPUB parsing
ebooklib>=0.18
beautifulsoup4>=4.12
lxml>=5.0

# Audio
soundfile>=0.13.1
numpy>=1.26

# Text processing
misaki>=0.7.10

# CLI / UX
tqdm>=4.66
rich>=13.0

# Testing
pytest>=8.0
